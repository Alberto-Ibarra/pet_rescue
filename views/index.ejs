<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Damion&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/03104b444a.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="nav">
        <nav class="nav1">
            <h1 class="title">PetRescue</h1>
        </nav>
        <nav class="nav2">
            <div class="nav2content">
                <a href="/pets/new" style="font-size: x-large;font-style: bold;">Add Pet</a>
                <form action="/pets/search" method="GET" class="form">
                    <input type="text" name="search" placeholder="Search..." class="search">
                    <button type="submit" class="searchbtn"><i class="fa-solid fa-bone fa-2xl"></i></button>
                </form>
            </div>
        </nav>
    </div>
    <img src="https://newevolutiondesigns.com/images/freebies/4k-dog-wallpaper-1.jpg" class="homeImg">
    <h1 style="text-align: center;">Adopt your next best friend!</h1>
    <div class="grid">
    <% for(let i = 0; i < pets.length; i++){ %>
        <div class="card">
            <a href="/pets/<%= pets[i]._id %>"><img src="<%=pets[i].img%>" style="width: 100%;border-radius: 5px 5px 0 0;height: 250px;"></a>
            <div class="cardContainer">
                <h2><%=pets[i].name%></h2>
                <% if(pets[i].type == 'dog'){ %>
                    <i class="fa-solid fa-dog fa-2xl petIcon"></i>
                <% } else if(pets[i].type == 'cat') {%>
                    <i class="fa-solid fa-cat fa-2xl petIcon"></i>
                <% }else if(pets[i].type == 'bird') {%>
                    <i class="fa-solid fa-dove fa-2xl petIcon"></i>
                <% }else if(pets[i].type == 'rabbit') {%>
                    <i class="fa-solid fa-carrot petIcon"></i>
                <% }else{ %>
                    <%=pets[i].type %>
                <% } %>
            </div>
        </div>
        <%}%>
    </div>
</body>
</html>